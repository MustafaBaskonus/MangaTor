@model IEnumerable<CategoryWithTotalChapteDto>
@*
<div class="mt-3">
    <h6 class="lead">
        <i class="fa fa-solid fa-bars text-secondary"></i>
        Categories
    </h6>
</div>

<div class="my-4">
    <ul class="list-group">
        @foreach (var cat in Model)
        {
            <li class="list-group-item">
                <a asp-action="Index" asp-route-categoryId="@cat.CategoryId">
                    @cat.Name (@cat.TotalComic)
                </a>
            </li>
        }
    </ul>
</div>
*@


<div class="heading-section">
    <i class="fa fa-solid fa-bars text-secondary"><h6>Categories</h6></i>
</div>

@foreach (var cat in Model)
{
    <div class="card-text">
        <a asp-action="Index" asp-route-categoryId="@cat.CategoryId">
            @cat.Name (@cat.TotalComic)
        </a>
    </div>
}


